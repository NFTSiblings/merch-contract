{
  "extends": ["solhint:recommended"],
  "rules": {
    "prettier/prettier": "error",
    "compiler-version": ["error", "^0.8.7"],
    "avoid-throw": false,
    "avoid-suicide": "error",
    "avoid-sha3": "warn"
  }
}
